# No custom keep rules needed yet — the release build does not minify.
# When R8 is enabled in a later phase, kotlinx-serialization and Filament
# consumer rules (shipped inside the AARs) cover the reflective entry points.
